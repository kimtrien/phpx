name: Build and Push Multi-Arch Docker Images

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write
    
    strategy:
      matrix:
        php_version: ['8.4', '8.5']
        frankenphp_version: ['1.12']

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to Docker Hub
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Log in to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Determine if latest tag
        id: latest
        run: |
          if [ "${{ matrix.php_version }}" = "8.5" ] && [ "${{ github.ref }}" = "refs/heads/main" ]; then
            echo "is_latest=true" >> $GITHUB_OUTPUT
          else
            echo "is_latest=false" >> $GITHUB_OUTPUT
          fi
          
          # Check if this is the default PHP version for version tags
          if [ "${{ matrix.php_version }}" = "8.5" ] && [[ "${{ github.ref }}" == refs/tags/* ]]; then
            echo "is_default_php=true" >> $GITHUB_OUTPUT
          else
            echo "is_default_php=false" >> $GITHUB_OUTPUT
          fi

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: |
            kimtrien/phpx
            ghcr.io/kimtrien/phpx
          tags: |
            type=raw,value=latest,enable=${{ steps.latest.outputs.is_latest }}
            type=raw,value=php${{ matrix.php_version }}
            type=raw,value=php${{ matrix.php_version }}-frankenphp${{ matrix.frankenphp_version }}
            type=semver,pattern={{version}},enable=${{ steps.latest.outputs.is_default_php }}
            type=semver,pattern={{version}},suffix=-php${{ matrix.php_version }}
            type=semver,pattern={{major}}.{{minor}},enable=${{ steps.latest.outputs.is_default_php }}
            type=semver,pattern={{major}}.{{minor}},suffix=-php${{ matrix.php_version }}
            type=semver,pattern=v{{version}},enable=${{ steps.latest.outputs.is_default_php }}
            type=semver,pattern=v{{version}},suffix=-php${{ matrix.php_version }}
            type=sha,prefix=php${{ matrix.php_version }}-
          labels: |
            org.opencontainers.image.title=PHPX
            org.opencontainers.image.description=Production-ready PHP base image powered by FrankenPHP with pre-installed extensions
            org.opencontainers.image.vendor=${{ github.repository_owner }}
            org.opencontainers.image.licenses=MIT

      - name: Build and push Docker image
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            PHP_VERSION=${{ matrix.php_version }}
            FRANKENPHP_VERSION=${{ matrix.frankenphp_version }}
          cache-from: type=gha,scope=php${{ matrix.php_version }}
          cache-to: type=gha,mode=max,scope=php${{ matrix.php_version }}

      - name: Generate artifact attestation
        if: github.event_name != 'pull_request'
        uses: actions/attest-build-provenance@v1
        with:
          subject-name: ghcr.io/kimtrien/phpx
          subject-digest: ${{ steps.build.outputs.digest }}
          push-to-registry: true
